# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'rhb registration' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for rhb registration
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'EVReflection'
  pod 'JGProgressHUD'

  target 'rhb registrationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'rhb registrationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
